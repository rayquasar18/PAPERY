---
plan: 09-04
title: "Layout Shell — Sidebar, Top Bar, Chat Panel & Responsive"
phase: 9
wave: 3
depends_on: [09-02, 09-03]
requirements: [FRONT-04, FRONT-09]
files_modified:
  - frontend/src/components/layout/app-sidebar.tsx
  - frontend/src/components/layout/top-bar.tsx
  - frontend/src/components/layout/chat-panel.tsx
  - frontend/src/components/layout/theme-toggle.tsx
  - frontend/src/components/layout/language-switcher.tsx
  - frontend/src/components/layout/user-menu.tsx
  - frontend/src/app/[locale]/(dashboard)/layout.tsx
  - frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx
  - frontend/src/app/[locale]/(dashboard)/default.tsx
  - frontend/src/lib/hooks/use-media-query.ts
autonomous: true
estimated_minutes: 30
---

# Plan 09-04: Layout Shell — Sidebar, Top Bar, Chat Panel & Responsive

## Goal

Build the complete application layout shell: collapsible sidebar with navigation items, sticky top bar with breadcrumb/search/theme/user controls, resizable chat panel, and responsive behavior (mobile sheet, tablet icon-only, desktop expanded).

## Requirement Coverage

| Requirement | Coverage |
|-------------|----------|
| FRONT-04 | Responsive layout: mobile (sheet sidebar), tablet (icon-only), desktop (expanded) |
| FRONT-09 | shadcn/ui Sidebar component, Sheet, Breadcrumb, DropdownMenu used |

## Tasks

<task id="09-04-01">
<title>Install layout-specific shadcn components and react-resizable-panels</title>
<read_first>
- frontend/package.json (current dependencies)
- .planning/phases/09-frontend-foundation-auth-ui/09-RESEARCH-layout-ui.md (shadcn components list, react-resizable-panels)
- .planning/phases/09-frontend-foundation-auth-ui/09-UI-SPEC.md (component inventory)
</read_first>
<action>
Run in `frontend/`:
```bash
pnpm add react-resizable-panels cmdk
pnpm dlx shadcn@latest add sidebar breadcrumb sheet badge scroll-area tabs skeleton alert-dialog command resizable toggle toggle-group
```
</action>
<acceptance_criteria>
- `frontend/package.json` contains `react-resizable-panels`
- `frontend/src/components/ui/sidebar.tsx` exists
- `frontend/src/components/ui/breadcrumb.tsx` exists
- `frontend/src/components/ui/sheet.tsx` exists
- `frontend/src/components/ui/skeleton.tsx` exists
- `frontend/src/components/ui/command.tsx` exists
</acceptance_criteria>
</task>

<task id="09-04-02">
<title>Create AppSidebar component</title>
<read_first>
- frontend/src/components/ui/sidebar.tsx (shadcn sidebar component API)
- .planning/phases/09-frontend-foundation-auth-ui/09-RESEARCH-layout-ui.md (sidebar architecture, component hierarchy, collapsible modes)
- .planning/phases/09-frontend-foundation-auth-ui/09-UI-SPEC.md (sidebar spec — 280px expanded, 56px collapsed, nav items: Dashboard/Projects/Settings)
- .planning/phases/09-frontend-foundation-auth-ui/09-CONTEXT.md (D-01, D-02, D-03, D-04, D-07)
</read_first>
<action>
Create `frontend/src/components/layout/app-sidebar.tsx`:
1. Use shadcn `Sidebar` with `collapsible="icon"` (D-02).
2. `SidebarHeader` with PAPERY logo and app name.
3. `SidebarContent` with `SidebarGroup` containing navigation items:
   - Dashboard (LayoutDashboard icon) → `/{locale}/dashboard`
   - Projects (FolderKanban icon) → `/{locale}/projects`
   - Settings (Settings icon) → `/{locale}/settings`
4. Use `SidebarMenuButton` with `asChild` and `Link` from `@/i18n/navigation`.
5. `SidebarFooter` with user info (avatar + name) — placeholder.
6. `SidebarRail` for the toggle rail.
7. Active state using `usePathname` from `@/i18n/navigation`.
</action>
<acceptance_criteria>
- `frontend/src/components/layout/app-sidebar.tsx` contains `collapsible="icon"`
- `frontend/src/components/layout/app-sidebar.tsx` contains `SidebarHeader`
- `frontend/src/components/layout/app-sidebar.tsx` contains `SidebarContent`
- `frontend/src/components/layout/app-sidebar.tsx` contains `LayoutDashboard`
- `frontend/src/components/layout/app-sidebar.tsx` contains `FolderKanban`
- `frontend/src/components/layout/app-sidebar.tsx` contains `SidebarRail`
- `frontend/src/components/layout/app-sidebar.tsx` contains `Link` from `@/i18n/navigation`
</acceptance_criteria>
</task>

<task id="09-04-03">
<title>Create TopBar with theme toggle, language switcher, and user menu</title>
<read_first>
- .planning/phases/09-frontend-foundation-auth-ui/09-UI-SPEC.md (top bar spec — 56px height, sticky, content layout)
- .planning/phases/09-frontend-foundation-auth-ui/09-RESEARCH-theme-design.md (ThemeToggle component code)
- .planning/phases/09-frontend-foundation-auth-ui/09-RESEARCH-i18n.md (LanguageSwitcher code)
- .planning/phases/09-frontend-foundation-auth-ui/09-CONTEXT.md (D-16, D-21)
- frontend/src/components/ui/dropdown-menu.tsx (shadcn dropdown API)
</read_first>
<action>
1. Create `frontend/src/components/layout/theme-toggle.tsx`:
   - DropdownMenu with Sun/Moon icons
   - Three options: Light, Dark, System (using `useTheme` from next-themes)
   - Mounted check to avoid SSR mismatch
   - Use `useTranslations('Theme')` for labels

2. Create `frontend/src/components/layout/language-switcher.tsx`:
   - DropdownMenu showing current locale
   - Options: English, Tiếng Việt
   - Use `useRouter().replace(pathname, { locale: nextLocale })` from `@/i18n/navigation`

3. Create `frontend/src/components/layout/user-menu.tsx`:
   - DropdownMenu triggered by Avatar
   - Menu items: Profile, Settings, Logout
   - Placeholder — full auth integration in 09-05

4. Create `frontend/src/components/layout/top-bar.tsx`:
   - 56px height, sticky top-0, z-40, border-b
   - Left: `SidebarTrigger` + Separator + Breadcrumb
   - Right: ThemeToggle + LanguageSwitcher + ChatPanel toggle button + UserMenu
</action>
<acceptance_criteria>
- `frontend/src/components/layout/theme-toggle.tsx` contains `useTheme`
- `frontend/src/components/layout/theme-toggle.tsx` contains `setTheme`
- `frontend/src/components/layout/language-switcher.tsx` contains `useRouter`
- `frontend/src/components/layout/language-switcher.tsx` contains `locale`
- `frontend/src/components/layout/top-bar.tsx` contains `SidebarTrigger`
- `frontend/src/components/layout/top-bar.tsx` contains `ThemeToggle`
- `frontend/src/components/layout/top-bar.tsx` contains `h-14` or `height: 56px` or `3.5rem`
- `frontend/src/components/layout/user-menu.tsx` exists
</acceptance_criteria>
</task>

<task id="09-04-04">
<title>Create ChatPanel and useMediaQuery hook</title>
<read_first>
- .planning/phases/09-frontend-foundation-auth-ui/09-UI-SPEC.md (chat panel spec — 480px default, 320px min, 50% max)
- .planning/phases/09-frontend-foundation-auth-ui/09-RESEARCH-layout-ui.md (react-resizable-panels chat panel code, useMediaQuery hook)
- frontend/src/lib/stores/sidebar-store.ts (isChatPanelOpen, toggleChatPanel)
</read_first>
<action>
1. Create `frontend/src/lib/hooks/use-media-query.ts`:
   ```typescript
   import { useState, useEffect } from 'react';

   export function useMediaQuery(query: string): boolean {
     const [matches, setMatches] = useState(false);
     useEffect(() => {
       const media = window.matchMedia(query);
       setMatches(media.matches);
       const listener = (e: MediaQueryListEvent) => setMatches(e.matches);
       media.addEventListener('change', listener);
       return () => media.removeEventListener('change', listener);
     }, [query]);
     return matches;
   }
   ```

2. Create `frontend/src/components/layout/chat-panel.tsx`:
   - Placeholder card: "AI Assistant" title, "AI-powered chat will be available in a future update." body
   - Uses `useTranslations('Chat')` for i18n
   - Has a header with close button
   - Uses `ScrollArea` for content area
</action>
<acceptance_criteria>
- `frontend/src/lib/hooks/use-media-query.ts` contains `export function useMediaQuery`
- `frontend/src/lib/hooks/use-media-query.ts` contains `window.matchMedia`
- `frontend/src/components/layout/chat-panel.tsx` exists
- `frontend/src/components/layout/chat-panel.tsx` contains `useTranslations`
</acceptance_criteria>
</task>

<task id="09-04-05">
<title>Create dashboard layout with sidebar + chat panel composition</title>
<read_first>
- frontend/src/components/layout/app-sidebar.tsx (created in 09-04-02)
- frontend/src/components/layout/top-bar.tsx (created in 09-04-03)
- frontend/src/components/layout/chat-panel.tsx (created in 09-04-04)
- .planning/phases/09-frontend-foundation-auth-ui/09-RESEARCH-layout-ui.md (overall layout composition, provider nesting, PanelGroup)
- .planning/phases/09-frontend-foundation-auth-ui/09-UI-SPEC.md (layout contract, responsive breakpoints)
</read_first>
<action>
1. Create `frontend/src/app/[locale]/(dashboard)/layout.tsx`:
   - `SidebarProvider` wrapping entire layout
   - `AppSidebar` component
   - Main area with `TopBar` at top
   - `PanelGroup direction="horizontal"` for main content + chat panel
   - Main content panel with `{children}`
   - Conditional chat panel based on `useSidebarStore().isChatPanelOpen`
   - `PanelResizeHandle` between main and chat panel

2. Create `frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx`:
   - Simple placeholder dashboard page
   - Empty state: "No projects yet" card with "Create project" button
   - Use `useTranslations('Dashboard')` for i18n

3. Create `frontend/src/app/[locale]/(dashboard)/default.tsx`:
   - Required for parallel routes in Next.js 16
   - Returns `null`
</action>
<acceptance_criteria>
- `frontend/src/app/[locale]/(dashboard)/layout.tsx` contains `SidebarProvider`
- `frontend/src/app/[locale]/(dashboard)/layout.tsx` contains `AppSidebar`
- `frontend/src/app/[locale]/(dashboard)/layout.tsx` contains `TopBar`
- `frontend/src/app/[locale]/(dashboard)/layout.tsx` contains `PanelGroup`
- `frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx` exists
- `frontend/src/app/[locale]/(dashboard)/default.tsx` exists
</acceptance_criteria>
</task>

## Threat Model

<threat_model>
| Threat | Severity | Likelihood | Mitigation |
|--------|----------|------------|------------|
| XSS in sidebar navigation | LOW | LOW | Links use next-intl Link, no dynamic href from user input |
| Clickjacking on chat panel | LOW | LOW | Standard CSP headers (handled at infra level) |
</threat_model>

## Verification

```bash
cd frontend && pnpm build
# Visit /en/dashboard — full layout visible with sidebar, top bar, main content
# Toggle sidebar — collapses to icon-only
# Toggle chat panel — opens from right with placeholder
# Resize browser — responsive behavior (mobile sheet, tablet icon-only)
# Theme toggle works
# Language switcher changes locale
```

## must_haves

- [ ] AppSidebar with collapsible icon mode and 3 nav items
- [ ] TopBar with theme toggle, language switcher, user menu
- [ ] ChatPanel with placeholder content
- [ ] Dashboard layout composing sidebar + topbar + main + chat panel
- [ ] Responsive: mobile sheet, tablet icon-only, desktop expanded
- [ ] useMediaQuery hook
- [ ] `pnpm build` succeeds
