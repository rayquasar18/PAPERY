---
phase: 10-dashboard-admin-ui-quasarflow-stubs
plan: 06
type: execute
wave: 1
depends_on:
  - 03
files_modified:
  - frontend/src/app/[locale]/(admin)/admin/layout.tsx
  - frontend/src/app/[locale]/(admin)/admin/users/page.tsx
  - frontend/src/app/[locale]/(admin)/admin/tiers/page.tsx
  - frontend/src/app/[locale]/(admin)/admin/rate-limits/page.tsx
  - frontend/src/app/[locale]/(admin)/admin/settings/page.tsx
  - frontend/src/lib/api/admin.ts
  - frontend/src/schemas/admin.schemas.ts
  - frontend/src/lib/auth/get-session-user.ts
autonomous: true
gap_closure: true
requirements:
  - ADMIN-01
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06
must_haves:
  truths:
    - "Admin route boundary rejects non-superusers at the frontend shell, not just unauthenticated users"
    - "Users, tiers, rate-limits, and settings screens load real backend data for production-ready basic list/detail/edit flows"
    - "Admin tiers page no longer relies on hardcoded tier drafts"
  artifacts:
    - path: "frontend/src/app/[locale]/(admin)/admin/layout.tsx"
      provides: "Superuser-enforced admin shell"
    - path: "frontend/src/app/[locale]/(admin)/admin/tiers/page.tsx"
      provides: "Backend-driven tier management surface"
    - path: "frontend/src/lib/auth/get-session-user.ts"
      provides: "Shared server-side session/user role lookup for route guards"
  key_links:
    - from: "frontend/src/app/[locale]/(admin)/admin/layout.tsx"
      to: "backend/app/api/v1/admin/__init__.py"
      via: "frontend superuser gate aligned to backend superuser-only router"
      pattern: "/admin"
    - from: "frontend/src/lib/api/admin.ts"
      to: "backend/app/api/v1/admin/tiers.py"
      via: "typed list/create/update tier calls"
      pattern: "/admin/tiers"
---

<objective>
Close the admin UI verification gap by enforcing a true superuser guard and replacing static/basic admin modules with backend-driven flows.

Purpose: satisfy D-05 and D-06 exactly so the admin surface is both correctly protected and production-ready basic across core modules.
Output: a superuser-enforced admin shell plus live users/tiers/rate-limits/settings screens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-CONTEXT.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VERIFICATION.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-03-SUMMARY.md
@backend/app/api/v1/admin/__init__.py
@backend/app/api/v1/admin/tiers.py
@frontend/src/app/[locale]/(admin)/admin/layout.tsx
@frontend/src/lib/api/admin.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce superuser-only guard at the admin route shell</name>
  <files>frontend/src/app/[locale]/(admin)/admin/layout.tsx, frontend/src/lib/auth/get-session-user.ts</files>
  <read_first>
    - frontend/src/app/[locale]/(admin)/admin/layout.tsx
    - backend/app/api/v1/admin/__init__.py
    - .planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: unauthenticated users are redirected away from admin routes.
    - Test 2: authenticated non-superusers are also redirected/blocked at the admin shell per D-05.
    - Test 3: superusers can proceed without duplicating a separate frontend app, preserving D-04.
  </behavior>
  <action>Replace the current access-token-only check in `admin/layout.tsx` with a real server-side superuser gate aligned to the backend admin router contract. Add a shared session/user lookup helper if needed so the layout can determine `is_superuser` before rendering children. Preserve the single shared Next.js codebase decision from D-04 and do not rely on client-only role checks or decorative warning banners as substitutes for enforcement.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/frontend && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>The admin layout source no longer grants access based solely on `access_token` presence and includes an explicit superuser authorization path.</acceptance_criteria>
  <done>Frontend admin boundary now matches D-05 and the backend superuser-only contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace static/basic admin modules with backend-driven list/detail/edit flows</name>
  <files>frontend/src/app/[locale]/(admin)/admin/users/page.tsx, frontend/src/app/[locale]/(admin)/admin/tiers/page.tsx, frontend/src/app/[locale]/(admin)/admin/rate-limits/page.tsx, frontend/src/app/[locale]/(admin)/admin/settings/page.tsx, frontend/src/lib/api/admin.ts, frontend/src/schemas/admin.schemas.ts</files>
  <read_first>
    - frontend/src/app/[locale]/(admin)/admin/users/page.tsx
    - frontend/src/app/[locale]/(admin)/admin/tiers/page.tsx
    - frontend/src/lib/api/admin.ts
    - backend/app/api/v1/admin/__init__.py
    - backend/app/api/v1/admin/tiers.py
    - .planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: users page renders real backend data rather than fallback-only copy when API is available.
    - Test 2: tiers page removes `tierDrafts` and loads live tier records plus edit/create actions.
    - Test 3: rate-limits and settings pages also use backend responses for production-ready basic list/detail/edit scope per D-06.
  </behavior>
  <action>Upgrade the four admin modules to backend-driven basic production surfaces per D-06: users, tiers, rate-limits, and settings must all use typed admin API calls and schema validation. Expand `admin.ts` and `admin.schemas.ts` where required so list/detail/edit flows are concrete and consistent with backend contracts. Remove `tierDrafts` and any equivalent static fallback datasets. Safe empty/error states are fine, but they must represent API outcomes rather than substituting hardcoded content.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/frontend && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>Admin pages are backed by live API calls for users, tiers, rate limits, and settings, and the tiers screen contains no hardcoded `tierDrafts` data.</acceptance_criteria>
  <done>Verification gap #2 is closed with true superuser enforcement and backend-driven core admin modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→admin shell | Potential privilege escalation by authenticated non-superusers |
| admin screens→admin APIs | High-privilege mutations cross from UI into protected backend routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-20 | E | `frontend/src/app/[locale]/(admin)/admin/layout.tsx` | mitigate | Enforce server-side superuser check before rendering children and pair with backend router dependency |
| T-10-21 | T | `frontend/src/lib/api/admin.ts` | mitigate | Parse admin responses with zod and keep mutation payloads constrained to backend contract |
| T-10-22 | I | admin fallback states | mitigate | Use generic load/error states that do not reveal privileged data when authorization fails |
</threat_model>

<verification>
Run `cd frontend && pnpm lint && pnpm build`, then verify the admin layout no longer authorizes by cookie presence alone and the tiers/users/rate-limits/settings screens all consume backend APIs.
</verification>

<success_criteria>
- Verification gap #2 is closed in full.
- D-04, D-05, and D-06 remain fully honored.
- Admin tiers page no longer uses hardcoded `tierDrafts`.
</success_criteria>

<output>
After completion, create `/Users/mqcbook/Documents/github/my-source/PAPERY/.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-06-SUMMARY.md`
</output>
