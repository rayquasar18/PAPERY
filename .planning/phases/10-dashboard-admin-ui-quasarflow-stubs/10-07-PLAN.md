---
phase: 10-dashboard-admin-ui-quasarflow-stubs
plan: 07
type: execute
wave: 2
depends_on:
  - 02
  - 03
files_modified:
  - frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx
  - frontend/src/components/dashboard/ai-job-runner.tsx
  - frontend/src/lib/api/ai-jobs.ts
  - frontend/src/schemas/ai-job.schemas.ts
autonomous: true
gap_closure: true
requirements:
  - QFLOW-04
must_haves:
  truths:
    - "Dashboard can submit an AI job to the backend from the UI"
    - "Dashboard polls live job status until a terminal state per D-07"
    - "Dashboard renders real pending/running/succeeded/failed/timed_out transitions instead of static recentJobs"
    - "Failed/timed_out jobs expose manual retry entry points per D-08"
  artifacts:
    - path: "frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx"
      provides: "Live AI job submit-and-poll dashboard surface"
    - path: "frontend/src/components/dashboard/ai-job-runner.tsx"
      provides: "Client polling orchestration and status rendering"
    - path: "frontend/src/lib/api/ai-jobs.ts"
      provides: "Submit/status helpers with terminal-state utility"
  key_links:
    - from: "frontend/src/components/dashboard/ai-job-runner.tsx"
      to: "backend/app/api/v1/ai_jobs.py"
      via: "submit and polling calls"
      pattern: "POST /ai-jobs|GET /ai-jobs/"
    - from: "frontend/src/lib/api/ai-jobs.ts"
      to: "frontend/src/schemas/ai-job.schemas.ts"
      via: "zod-validated polling responses"
      pattern: "pending|running|succeeded|failed|timed_out"
---

<objective>
Close the AI job frontend gap by wiring dashboard submit-to-poll behavior against the existing backend async job contract.

Purpose: satisfy the failed end-to-end async simulation truth without changing the polling-first D-07 decision or the light-retry D-08 policy.
Output: a live dashboard AI job surface that submits work, polls status, renders transitions, and supports manual retry after terminal failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-CONTEXT.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VERIFICATION.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-03-SUMMARY.md
@backend/app/api/v1/ai_jobs.py
@frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx
@frontend/src/lib/api/ai-jobs.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace static recentJobs with live submit and polling components</name>
  <files>frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx, frontend/src/components/dashboard/ai-job-runner.tsx, frontend/src/lib/api/ai-jobs.ts, frontend/src/schemas/ai-job.schemas.ts</files>
  <read_first>
    - frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx
    - frontend/src/lib/api/ai-jobs.ts
    - backend/app/api/v1/ai_jobs.py
    - .planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: dashboard no longer renders a local `recentJobs` constant.
    - Test 2: a dashboard action submits a real AI job and stores the returned job id/status.
    - Test 3: UI polls status until terminal state, then stops automatically per D-07.
  </behavior>
  <action>Remove the hardcoded `recentJobs` array from `dashboard/page.tsx` and introduce a focused client component for AI job submission and polling. Use the existing `aiJobsApi.submit` and `aiJobsApi.getStatus` helpers against the backend contract from Plan 02. Keep the transport polling-first per D-07 and structure the component so a future SSE transport can replace the polling loop without rewriting the display contract. Do not fake intermediate states locally except for honest optimistic request boundaries between submit response and first poll result.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/frontend && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>The dashboard source contains no hardcoded `recentJobs` dataset and instead renders status cards from live API-backed job state.</acceptance_criteria>
  <done>Live submit-to-poll rendering path exists on the dashboard and reflects backend statuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add terminal-state UX and manual retry flow</name>
  <files>frontend/src/components/dashboard/ai-job-runner.tsx, frontend/src/lib/api/ai-jobs.ts, frontend/src/schemas/ai-job.schemas.ts</files>
  <read_first>
    - .planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-CONTEXT.md
    - frontend/src/components/dashboard/ai-job-runner.tsx
    - frontend/src/lib/api/ai-jobs.ts
  </read_first>
  <behavior>
    - Test 1: polling halts on `succeeded`, `failed`, or `timed_out` terminal states.
    - Test 2: failed/timed_out jobs present a manual retry action consistent with D-08.
    - Test 3: status text and badges reflect real backend transitions, including timeout/failure detail when present.
  </behavior>
  <action>Complete the frontend async UX around D-08 by stopping polling on terminal states and surfacing a manual retry action that simply submits a new job through the same backend contract. Reuse validated schema fields for terminal-state messaging and avoid inventing client-only retry counters that diverge from backend job history. Keep retry semantics light and explicit: bounded backend retries happen automatically, while the UI only offers manual re-submit after the terminal state is visible.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/frontend && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>Dashboard AI job UI visibly progresses through live statuses and exposes manual retry only after terminal failure/timeout.</acceptance_criteria>
  <done>Verification gap #3 is fully closed with end-to-end frontend submit, polling, transition rendering, and manual retry UX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→AI jobs API | User-triggered actions enqueue background work and can be spammed or malformed |
| polling loop→status endpoint | Repeated status checks can leak state if client handling is careless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-23 | D | `frontend/src/components/dashboard/ai-job-runner.tsx` | mitigate | Use bounded polling intervals and stop automatically on terminal states |
| T-10-24 | T | `frontend/src/lib/api/ai-jobs.ts` | mitigate | Validate submit/status payloads with zod before rendering or branching on status |
| T-10-25 | I | dashboard job history cards | mitigate | Render only caller-owned status data returned by backend and do not cache/share across users |
</threat_model>

<verification>
Run `cd frontend && pnpm lint && pnpm build`, then verify the dashboard submits a real AI job, polls until terminal state, and shows manual retry after failed/timed_out outcomes.
</verification>

<success_criteria>
- Verification gap #3 is closed in full.
- D-07 polling-first transport is preserved.
- D-08 light retry semantics are preserved with backend auto-retries plus UI manual retry after terminal failure.
</success_criteria>

<output>
After completion, create `/Users/mqcbook/Documents/github/my-source/PAPERY/.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-07-SUMMARY.md`
</output>
