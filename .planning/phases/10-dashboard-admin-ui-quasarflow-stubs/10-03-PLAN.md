---
phase: 10-dashboard-admin-ui-quasarflow-stubs
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - frontend/src/proxy.ts
  - frontend/src/app/[locale]/(dashboard)/projects/page.tsx
  - frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx
  - frontend/src/app/[locale]/(admin)/layout.tsx
  - frontend/src/app/[locale]/(admin)/users/page.tsx
  - frontend/src/app/[locale]/(admin)/tiers/page.tsx
  - frontend/src/app/[locale]/(admin)/rate-limits/page.tsx
  - frontend/src/app/[locale]/(admin)/settings/page.tsx
  - frontend/src/lib/api/ai-jobs.ts
  - frontend/src/lib/api/admin.ts
  - frontend/src/schemas/ai-job.schemas.ts
  - frontend/src/schemas/admin.schemas.ts
autonomous: false
requirements:
  - QFLOW-04
must_haves:
  truths:
    - "Dashboard project screen supports list/table-first view with switchable cards"
    - "Dashboard toolbar includes only search and sort controls"
    - "Empty project state provides clear CTA to create first project"
    - "Admin modules exist in same Next.js app but are isolated under /(admin) with superuser gate"
    - "Frontend can poll AI job status API and render terminal/non-terminal states"
  artifacts:
    - path: "frontend/src/app/[locale]/(dashboard)/projects/page.tsx"
      provides: "Hybrid project dashboard UI"
    - path: "frontend/src/app/[locale]/(admin)/layout.tsx"
      provides: "Admin route boundary and guard shell"
    - path: "frontend/src/app/[locale]/(admin)/users/page.tsx"
      provides: "Admin users module"
    - path: "frontend/src/lib/api/ai-jobs.ts"
      provides: "Polling-ready AI jobs API client"
  key_links:
    - from: "frontend/src/proxy.ts"
      to: "frontend/src/app/[locale]/(admin)/layout.tsx"
      via: "route protection and role boundary"
      pattern: "admin"
    - from: "frontend/src/app/[locale]/(dashboard)/projects/page.tsx"
      to: "frontend/src/lib/api/client.ts"
      via: "typed API usage for projects/search/sort"
      pattern: "search|sort|view"
    - from: "frontend/src/lib/api/ai-jobs.ts"
      to: "backend /api/v1/ai-jobs"
      via: "submit/status polling calls"
      pattern: "status|pending|running|succeeded|failed|timed_out"
---

<objective>
Deliver dashboard and admin front-end surfaces in the shared Next.js app, including polling-first AI job UX integration.

Purpose: Complete user-visible v1 surfaces while honoring locked UX and access-boundary decisions.
Output: Dashboard project management UI, admin modules (users/tiers/rate-limits/settings), route guards, and AI polling client wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-CONTEXT.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-RESEARCH.md
@frontend/src/app/[locale]/(dashboard)/layout.tsx
@frontend/src/app/[locale]/(dashboard)/layout-client.tsx
@frontend/src/components/layout/app-sidebar.tsx
@frontend/src/components/layout/top-bar.tsx
@frontend/src/lib/api/client.ts
@frontend/src/proxy.ts

<interfaces>
From frontend/src/lib/api/client.ts:
```typescript
export const apiClient = axios.create({
  baseURL: `${API_BASE_URL}/api/${API_VERSION}`,
  withCredentials: true,
});
```

From frontend/src/proxy.ts:
```typescript
const PROTECTED_ROUTE_ROOTS = ['/dashboard', '/projects', '/settings'] as const;
export async function proxy(request: NextRequest): Promise<NextResponse>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build dashboard project surface with locked hybrid UX and empty-state CTA</name>
  <files>frontend/src/app/[locale]/(dashboard)/projects/page.tsx, frontend/src/app/[locale]/(dashboard)/dashboard/page.tsx, frontend/src/lib/api/ai-jobs.ts, frontend/src/schemas/ai-job.schemas.ts</files>
  <behavior>
    - Test 1: Default project display is table/list with a working toggle to card view (D-01).
    - Test 2: Toolbar exposes only search and sort controls; no advanced filter controls appear (D-02).
    - Test 3: Empty state renders clear message and prominent create-project CTA (D-03).
  </behavior>
  <action>Implement dashboard project page honoring D-01, D-02, and D-03 exactly: list/table-first interaction with switch-to-card option; toolbar strictly Search + Sort only; and actionable empty state with visible create CTA. Wire data through existing typed API client and backend project endpoints (no hardcoded sample rows). Add AI job API helper and schema parsing for submit/status so dashboard actions can poll async task progress from Plan 02 (D-07). Avoid any deferred features (advanced filters, SSE-default transport).
</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/frontend && pnpm lint && pnpm build</automated>
  </verify>
  <done>Dashboard surface is production-ready basic, decision-compliant, and connected to real backend/polling APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add /(admin) route group and core full admin modules in shared app</name>
  <files>frontend/src/proxy.ts, frontend/src/app/[locale]/(admin)/layout.tsx, frontend/src/app/[locale]/(admin)/users/page.tsx, frontend/src/app/[locale]/(admin)/tiers/page.tsx, frontend/src/app/[locale]/(admin)/rate-limits/page.tsx, frontend/src/app/[locale]/(admin)/settings/page.tsx, frontend/src/lib/api/admin.ts, frontend/src/schemas/admin.schemas.ts</files>
  <behavior>
    - Test 1: Admin pages live in same Next.js app and do not create a separate admin frontend (D-04).
    - Test 2: Non-superuser cannot access /(admin) routes due to route/access guard (D-05).
    - Test 3: Users, tiers, rate-limits, settings pages each provide production-ready basic list/detail/edit flows (D-06).
  </behavior>
  <action>Create localized `(admin)` route group in current app (D-04), add superuser guard through route protection plus server-side defensive checks (D-05), and implement core full modules for users/tiers/rate-limits/settings at list/detail/edit level (D-06). Reuse shared layout components and typed API patterns; do not split into a separate SPA. Keep scope at production-ready basic operations and avoid deferred analytics-heavy add-ons.
</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/frontend && pnpm lint && pnpm build</automated>
  </verify>
  <done>Admin UI is isolated by route and access boundary inside the shared frontend and covers all four core modules.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate dashboard/admin UX and access boundaries in browser</name>
  <what-built>Hybrid dashboard UX, admin route partitioning, and polling-first async status rendering</what-built>
  <how-to-verify>
    1. Open localized dashboard projects page and confirm default table/list view, toggle to cards, and toolbar showing only search/sort.
    2. Trigger empty state (no projects) and confirm prominent create-project CTA is visible and actionable.
    3. Login as non-superuser and verify /(admin) routes are blocked/redirected.
    4. Login as superuser and verify users/tiers/rate-limits/settings pages load and allow basic list/detail/edit.
    5. Trigger an AI action and confirm status updates through polling until terminal state.
  </how-to-verify>
  <resume-signal>Type "approved" to continue or provide exact mismatches.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client→admin routes | Potential privilege escalation attempts from non-superusers |
| dashboard UI→API client | User-controlled search/sort inputs cross into backend query endpoints |
| polling UI→job status API | Repeated status reads can expose unauthorized job metadata if scoped incorrectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | E | `frontend/src/proxy.ts` + `(admin)` pages | mitigate | Enforce admin route guard and pair with backend superuser authorization for every admin API |
| T-10-10 | I | `frontend/src/lib/api/ai-jobs.ts` | mitigate | Validate status payloads with zod schema and never render raw unvalidated fields |
| T-10-11 | D | dashboard polling loop | mitigate | Use bounded poll interval/backoff from D-07/D-08 and stop polling on terminal states |
| T-10-12 | T | dashboard search/sort parameters | mitigate | Normalize and validate query params client-side and rely on backend validation for final enforcement |
</threat_model>

<verification>
Run `cd frontend && pnpm lint && pnpm build`; then execute the human checkpoint flow for UI/access verification.
</verification>

<success_criteria>
- D-01, D-02, D-03 implemented exactly on dashboard project surface.
- D-04, D-05, D-06 implemented exactly on admin route group and modules.
- QFLOW-04 frontend polling integration works against Plan 02 endpoints.
- Human checkpoint confirms UX and route guard behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-03-SUMMARY.md`
</output>
