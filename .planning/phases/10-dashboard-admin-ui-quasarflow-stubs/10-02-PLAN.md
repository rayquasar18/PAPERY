---
phase: 10-dashboard-admin-ui-quasarflow-stubs
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - backend/app/models/ai_job.py
  - backend/app/repositories/ai_job_repository.py
  - backend/app/services/ai_job_service.py
  - backend/app/api/v1/ai_jobs.py
  - backend/app/worker/tasks/ai_jobs.py
  - backend/app/worker/__init__.py
  - backend/tests/test_quasarflow_resilience.py
  - backend/tests/test_ai_job_flow.py
  - backend/tests/test_worker_tasks.py
autonomous: true
requirements:
  - QFLOW-03
  - QFLOW-04
  - INFRA-05
must_haves:
  truths:
    - "Submitting an AI action returns immediately with a job id and pending status"
    - "Worker processes queued AI jobs asynchronously and persists status transitions"
    - "Timeout and bounded retry policy eventually lands in a terminal status with retriable signal"
    - "Frontend can poll job status endpoint to observe pending/running/succeeded/failed/timed_out"
  artifacts:
    - path: "backend/app/api/v1/ai_jobs.py"
      provides: "Submit/status API contract"
    - path: "backend/app/worker/tasks/ai_jobs.py"
      provides: "ARQ-executable job handlers"
    - path: "backend/app/repositories/ai_job_repository.py"
      provides: "Persistent status source of truth"
    - path: "backend/tests/test_ai_job_flow.py"
      provides: "End-to-end submit→queue→status tests"
  key_links:
    - from: "backend/app/api/v1/ai_jobs.py"
      to: "backend/app/services/ai_job_service.py"
      via: "submit/status handlers"
      pattern: "@router.post|@router.get"
    - from: "backend/app/worker/tasks/ai_jobs.py"
      to: "backend/app/services/quasarflow/stub_client.py"
      via: "background execution call"
      pattern: "retry|timeout|circuit"
    - from: "backend/app/services/ai_job_service.py"
      to: "backend/app/repositories/ai_job_repository.py"
      via: "status persistence"
      pattern: "pending|running|succeeded|failed|timed_out"
---

<objective>
Implement asynchronous AI orchestration via ARQ queue with polling-first status APIs and resilience rules.

Purpose: Deliver the operational async backbone required for QuasarFlow simulation and future real provider swaps.
Output: AI job model/repository/service, submit+status endpoints, worker tasks, and resilience test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-CONTEXT.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-RESEARCH.md
@backend/app/main.py
@backend/app/worker/__init__.py
@backend/app/api/v1/__init__.py
@backend/app/services/project_service.py
@backend/app/repositories/base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create persistent AI job domain and polling-first API contract</name>
  <files>backend/app/models/ai_job.py, backend/app/repositories/ai_job_repository.py, backend/app/services/ai_job_service.py, backend/app/api/v1/ai_jobs.py, backend/tests/test_ai_job_flow.py</files>
  <behavior>
    - Test 1: POST submit endpoint returns `{job_id, status:"pending"}` without waiting for execution.
    - Test 2: GET status endpoint returns only caller-owned job details and current status progression.
    - Test 3: Invalid or foreign job_id does not disclose job existence details to unauthorized callers.
  </behavior>
  <action>Write failing integration tests first in `test_ai_job_flow.py` for submit/status workflow per D-07 and QFLOW-04. Create `ai_job.py` model and repository for persisted job metadata/result/error with UUID job IDs. Implement service and `/api/v1/ai-jobs` + `/api/v1/ai-jobs/{job_id}` routes (polling-first per D-07). Ensure API contracts remain extensible to SSE in future without changing job schema shape. Enforce ownership checks on status reads and use consistent structured errors.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_ai_job_flow.py -q -k "submit or status"</automated>
  </verify>
  <done>Polling-first async contract is live with persistent job state and authorization-safe reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ARQ worker tasks with timeout, bounded retry, and circuit-breaker behavior</name>
  <files>backend/app/worker/tasks/ai_jobs.py, backend/app/worker/__init__.py, backend/app/services/ai_job_service.py, backend/tests/test_quasarflow_resilience.py, backend/tests/test_worker_tasks.py, backend/tests/test_ai_job_flow.py</files>
  <behavior>
    - Test 1: Worker moves job status pending→running→succeeded on successful stub call.
    - Test 2: Timeout/retry follows bounded backoff and ends in failed or timed_out terminal state per D-08.
    - Test 3: Circuit breaker opens after configured consecutive failures and fast-fails subsequent jobs until recovery window.
  </behavior>
  <action>Write/extend resilience tests first (`test_quasarflow_resilience.py`, `test_worker_tasks.py`) for D-08 and QFLOW-03. Implement executable ARQ task handlers under `worker/tasks/ai_jobs.py` and export registration from `worker/__init__.py` to satisfy INFRA-05. In service/worker execution, apply explicit timeout, bounded retries with backoff, and circuit-breaker state transitions; after bounds are reached, persist terminal status with manual-retry-friendly metadata (per D-08). Do not introduce unbounded loops or synchronous blocking request paths.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_quasarflow_resilience.py tests/test_worker_tasks.py tests/test_ai_job_flow.py -q</automated>
  </verify>
  <done>ARQ processing is operational, resilient, and fully observable through terminal/non-terminal statuses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend client→`/api/v1/ai-jobs` | Untrusted users submit workload requests that trigger background execution |
| API/service→worker queue | Job metadata crosses async boundary and can be replayed or malformed |
| worker→provider stub | External-like execution path can fail, time out, or degrade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | D | `backend/app/worker/tasks/ai_jobs.py` | mitigate | Enforce explicit timeout and bounded retry/backoff policy; no infinite retries |
| T-10-06 | I | `backend/app/api/v1/ai_jobs.py` status endpoint | mitigate | Require ownership checks for job reads and return non-disclosing errors on foreign IDs |
| T-10-07 | T | `backend/app/services/ai_job_service.py` status transitions | mitigate | Persist constrained enum transitions and reject illegal state rewrites |
| T-10-08 | E | worker task dispatch | mitigate | Restrict callable task registry in `worker/__init__.py`; do not execute arbitrary user-supplied task names |
</threat_model>

<verification>
Run `cd backend && uv run pytest tests/test_ai_job_flow.py tests/test_worker_tasks.py tests/test_quasarflow_resilience.py -q` and confirm async/resilience paths pass.
</verification>

<success_criteria>
- QFLOW-03 satisfied with explicit timeout/retry/circuit behavior and terminal failure states.
- QFLOW-04 satisfied with enqueue + polling status API contract.
- INFRA-05 satisfied with executable ARQ task worker wiring and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-02-SUMMARY.md`
</output>
