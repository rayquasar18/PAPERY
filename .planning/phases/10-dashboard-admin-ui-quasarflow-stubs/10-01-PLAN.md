---
phase: 10-dashboard-admin-ui-quasarflow-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/quasarflow/contracts.py
  - backend/app/services/quasarflow/stub_client.py
  - backend/app/schemas/ai_job.py
  - backend/tests/test_quasarflow_stub.py
autonomous: true
requirements:
  - QFLOW-01
  - QFLOW-02
must_haves:
  truths:
    - "Backend has a typed QuasarFlow abstraction that service code consumes without provider-specific imports"
    - "Stub provider returns realistic deterministic payloads for all supported AI actions"
    - "Response DTOs are validated consistently before leaving the service boundary"
  artifacts:
    - path: "backend/app/services/quasarflow/contracts.py"
      provides: "Abstract typed interface and result contracts"
    - path: "backend/app/services/quasarflow/stub_client.py"
      provides: "Development stub implementation"
    - path: "backend/app/schemas/ai_job.py"
      provides: "Canonical request/response schema for AI jobs"
    - path: "backend/tests/test_quasarflow_stub.py"
      provides: "Automated contract and realism verification"
  key_links:
    - from: "backend/app/services/quasarflow/stub_client.py"
      to: "backend/app/services/quasarflow/contracts.py"
      via: "class implementation of abstract methods"
      pattern: "class .*Stub.*\(.*QuasarFlow.*\)"
    - from: "backend/tests/test_quasarflow_stub.py"
      to: "backend/app/services/quasarflow/stub_client.py"
      via: "deterministic output assertions"
      pattern: "assert .*status|assert .*citations"
---

<objective>
Define the QuasarFlow integration boundary and deliver a realistic stub provider for development.

Purpose: Prevent lock-in to a concrete AI backend and enable immediate end-to-end testing before real QuasarFlow connectivity.
Output: Typed abstraction, validated DTOs, and deterministic stub behavior covered by tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-CONTEXT.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-RESEARCH.md
@backend/app/services/settings_service.py
@backend/app/api/v1/admin/settings.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define QuasarFlow contracts and schema boundary</name>
  <files>backend/app/services/quasarflow/contracts.py, backend/app/schemas/ai_job.py, backend/tests/test_quasarflow_stub.py</files>
  <behavior>
    - Test 1: Contract exposes typed methods for submit/process/status-oriented operations used by phase 10 AI flows.
    - Test 2: DTO schema rejects malformed provider output and accepts expected payload shape.
    - Test 3: Service modules can import contract types without importing concrete provider modules.
  </behavior>
  <action>Create `contracts.py` with an abstract base interface and explicit typed method signatures for AI operations per D-07 and QFLOW-01. Create `ai_job.py` schemas (status enum, payload, error detail) as the canonical boundary for provider responses. Write failing tests first in `test_quasarflow_stub.py` that assert method signatures, DTO validation, and import-level decoupling. Keep contract transport-agnostic so polling-first now can evolve to SSE later (per D-07) without contract rewrite.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_quasarflow_stub.py -q -k "contract or schema"</automated>
  </verify>
  <done>Typed abstraction exists, DTO schema is executable, and tests prove contract-first separation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic stub provider with realistic fake outputs</name>
  <files>backend/app/services/quasarflow/stub_client.py, backend/tests/test_quasarflow_stub.py</files>
  <behavior>
    - Test 1: Stub returns deterministic outputs for same input/job seed.
    - Test 2: Stub output includes realistic fields expected by UI/API (summary, citations, status metadata).
    - Test 3: Stub error shape follows canonical schema when simulated failure mode is requested.
  </behavior>
  <action>Implement `stub_client.py` to satisfy the abstract contract from Task 1 and return realistic fake data per QFLOW-02. Ensure determinism for repeatable tests (e.g., seeded generation by job_id), include citation-like structures and progress metadata required by later dashboard surfaces. Keep implementation in the same backend codebase (per D-04) and avoid placeholders like static one-line dummy responses; provide full fidelity realistic payloads.
</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_quasarflow_stub.py -q</automated>
  </verify>
  <done>Stub provider fully implements the contract and produces realistic deterministic development responses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/service→QuasarFlow provider abstraction | Untrusted provider-like payload crosses into trusted backend domain models |
| test fixtures→stub provider | Synthetic data paths can hide malformed payload assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `backend/app/services/quasarflow/stub_client.py` | mitigate | Validate all provider outputs against `backend/app/schemas/ai_job.py` before returning |
| T-10-02 | I | `backend/app/schemas/ai_job.py` | mitigate | Keep error payloads structured and avoid leaking internal stack traces through schema |
| T-10-03 | R | `backend/tests/test_quasarflow_stub.py` | mitigate | Add deterministic assertions for contract and payload fields so regressions are auditable |
| T-10-04 | E | `contracts.py` usage boundary | mitigate | Enforce interface-only imports in service code to avoid bypassing contract checks |
</threat_model>

<verification>
Run `cd backend && uv run pytest tests/test_quasarflow_stub.py -q` and confirm all contract + realism checks pass.
</verification>

<success_criteria>
- QFLOW-01 satisfied with typed abstract interface consumed by backend service code.
- QFLOW-02 satisfied with realistic deterministic stub implementation.
- Contract and stub are validated by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-01-SUMMARY.md`
</output>
