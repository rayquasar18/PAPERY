---
phase: 10-dashboard-admin-ui-quasarflow-stubs
plan: 04
type: execute
wave: 4
depends_on:
  - 02
  - 03
files_modified:
  - .github/workflows/verify.yml
  - frontend/package.json
  - backend/pyproject.toml
  - backend/Makefile
  - .planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VALIDATION.md
autonomous: true
requirements:
  - INFRA-13
must_haves:
  truths:
    - "GitHub Actions runs verify-only checks on push/PR without deployment steps"
    - "Pipeline enforces lint, type-check, tests, and build for both backend and frontend"
    - "Repository has one canonical local verify command path matching CI behavior"
  artifacts:
    - path: ".github/workflows/verify.yml"
      provides: "Verify-only CI workflow"
    - path: "backend/Makefile"
      provides: "Canonical backend verify target"
    - path: "frontend/package.json"
      provides: "Canonical frontend verify scripts"
  key_links:
    - from: ".github/workflows/verify.yml"
      to: "frontend/package.json"
      via: "frontend lint/type/build invocations"
      pattern: "lint|type|build"
    - from: ".github/workflows/verify.yml"
      to: "backend/Makefile"
      via: "backend verify target or equivalent command set"
      pattern: "ruff|mypy|pytest"
---

<objective>
Set up verify-only CI pipeline for phase 10 with strict quality gates and no deployment automation.

Purpose: Provide repeatable quality enforcement aligned with locked CI policy while keeping deployment out of scope.
Output: GitHub Actions verify workflow and aligned local verify commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-CONTEXT.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-RESEARCH.md
@backend/Makefile
@backend/pyproject.toml
@frontend/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical verify command surfaces for backend and frontend</name>
  <files>frontend/package.json, backend/Makefile, backend/pyproject.toml</files>
  <behavior>
    - Test 1: Frontend exposes lint/type/build commands callable in CI.
    - Test 2: Backend exposes lint/type/test verify sequence callable in CI.
    - Test 3: Local verify commands succeed in same order used by workflow.
  </behavior>
  <action>Align frontend and backend scripts/targets so CI can run verify-only checks consistently per D-09 and INFRA-13. Ensure command surfaces are explicit and stable (lint, type-check, tests, build) and avoid hidden side effects like deploy/release steps. If existing commands differ, normalize them without changing unrelated feature logic.
</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/frontend && pnpm lint && pnpm typecheck && pnpm build && cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run ruff check . && uv run mypy app/ && uv run pytest -q</automated>
  </verify>
  <done>Local verify commands are canonicalized and ready for GitHub Actions consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GitHub Actions verify-only workflow for PR/push</name>
  <files>.github/workflows/verify.yml, .planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VALIDATION.md</files>
  <behavior>
    - Test 1: Workflow triggers on push and pull_request.
    - Test 2: Workflow executes lint, type-check, test, build gates for both frontend/backend.
    - Test 3: Workflow contains no deploy/publish/release steps (D-09).
  </behavior>
  <action>Create `.github/workflows/verify.yml` implementing verify-only CI policy from D-09: run backend lint/type/test and frontend lint/type/build on PR and push. Keep workflow decomposition clear (single workflow with jobs or split jobs at your discretion) but do not add deployment automation, environment promotion, or secret-requiring publish stages. Record expected gate matrix and troubleshooting notes in `10-VALIDATION.md` for execution consistency.
</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY && gh workflow list && gh workflow view verify.yml --yaml</automated>
  </verify>
  <done>INFRA-13 verify-only CI is implemented and explicitly excludes deploy automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository push/PR→GitHub Actions runner | Untrusted code changes trigger CI execution environment |
| CI workflow→project command surfaces | Workflow relies on scripted commands that can drift or be bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | T | `.github/workflows/verify.yml` | mitigate | Pin verify steps explicitly (lint/type/test/build) and avoid dynamic shell indirection |
| T-10-14 | R | CI logs and traceability | mitigate | Keep deterministic command order and fail-fast behavior so failures are attributable |
| T-10-15 | E | workflow scope | mitigate | Exclude deploy/publish jobs per D-09 to minimize privileged token/secrets usage |
| T-10-16 | D | CI runtime exhaustion | accept | Verify-only checks may be heavier as phase grows; acceptable for quality gate, monitor via job timings |
</threat_model>

<verification>
Run local verify command chain, then inspect workflow YAML with `gh workflow view verify.yml --yaml` to confirm verify-only scope.
</verification>

<success_criteria>
- INFRA-13 satisfied with PR/push verify workflow.
- D-09 respected: no deployment automation in this phase.
- CI gates cover lint, type-check, tests, and build across backend/frontend.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-04-SUMMARY.md`
</output>
