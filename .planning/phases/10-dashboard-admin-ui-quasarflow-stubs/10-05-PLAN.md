---
phase: 10-dashboard-admin-ui-quasarflow-stubs
plan: 05
type: execute
wave: 1
depends_on:
  - 03
files_modified:
  - frontend/src/app/[locale]/(dashboard)/projects/page.tsx
  - frontend/src/lib/api/projects.ts
  - frontend/src/schemas/project.schemas.ts
  - frontend/src/components/dashboard/project-member-manager.tsx
  - frontend/src/components/dashboard/project-form-dialog.tsx
  - frontend/src/components/dashboard/project-delete-dialog.tsx
autonomous: true
gap_closure: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-05
  - PROJ-06
must_haves:
  truths:
    - "Projects dashboard loads real project data from backend instead of static demo rows"
    - "User can create, edit, and delete projects from the dashboard surface"
    - "Owner can update or remove project members from the dashboard surface"
    - "Dashboard preserves D-01 hybrid view, D-02 search+sort-only toolbar, and D-03 empty-state CTA while using live data"
  artifacts:
    - path: "frontend/src/app/[locale]/(dashboard)/projects/page.tsx"
      provides: "Live project dashboard with CRUD/member wiring"
    - path: "frontend/src/lib/api/projects.ts"
      provides: "Typed project CRUD/member API client"
    - path: "frontend/src/components/dashboard/project-member-manager.tsx"
      provides: "Member role/update/remove interactions"
  key_links:
    - from: "frontend/src/app/[locale]/(dashboard)/projects/page.tsx"
      to: "backend/app/api/v1/projects.py"
      via: "typed fetch and mutation calls"
      pattern: "/projects|/members/|/invites"
    - from: "frontend/src/components/dashboard/project-member-manager.tsx"
      to: "backend/app/services/project_service.py"
      via: "member role and remove mutations"
      pattern: "update_project_member|remove_project_member"
---

<objective>
Close the dashboard projects gap by replacing static demo data with real backend CRUD and member-management flows.

Purpose: satisfy the failed verification truth for the user dashboard while preserving locked UX decisions D-01, D-02, and D-03 exactly.
Output: a backend-driven projects page with create/edit/delete/member operations and no hardcoded dataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-CONTEXT.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VERIFICATION.md
@.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-03-SUMMARY.md
@backend/app/api/v1/projects.py
@backend/app/services/project_service.py
@frontend/src/app/[locale]/(dashboard)/projects/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace static project listing with typed backend query flow</name>
  <files>frontend/src/app/[locale]/(dashboard)/projects/page.tsx, frontend/src/lib/api/projects.ts, frontend/src/schemas/project.schemas.ts</files>
  <read_first>
    - frontend/src/app/[locale]/(dashboard)/projects/page.tsx
    - backend/app/api/v1/projects.py
    - backend/app/services/project_service.py
    - .planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: projects page reads backend list endpoint instead of `demoProjects` local data.
    - Test 2: toolbar still exposes only search and sort per D-02 while query params drive backend requests.
    - Test 3: empty state shows D-03 CTA only when live API returns zero rows, not from a fake toggle.
  </behavior>
  <action>Remove `demoProjects` and local-only filtering from `projects/page.tsx`. Create typed `projects` API helpers and zod schemas for list/detail payloads, then load data from the backend `GET /projects` contract with search and sort parameters mapped into the existing D-01 hybrid UX. Preserve the current list-first default plus card toggle per D-01, and do not add advanced filters or placeholder fallback rows because D-02 explicitly limits toolbar scope to Search + Sort only.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/frontend && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>Projects page source contains no hardcoded `demoProjects` dataset and renders backend-backed rows while preserving hybrid layout + search/sort-only behavior.</acceptance_criteria>
  <done>Live project list replaces static data and the page still matches D-01/D-02/D-03 exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire create, edit, delete, and member-management mutations</name>
  <files>frontend/src/app/[locale]/(dashboard)/projects/page.tsx, frontend/src/lib/api/projects.ts, frontend/src/components/dashboard/project-form-dialog.tsx, frontend/src/components/dashboard/project-delete-dialog.tsx, frontend/src/components/dashboard/project-member-manager.tsx</files>
  <read_first>
    - backend/app/api/v1/projects.py
    - backend/app/services/project_service.py
    - frontend/src/app/[locale]/(dashboard)/projects/page.tsx
    - .planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: create/edit/delete mutations call real backend endpoints and refresh the live list.
    - Test 2: member manager can change roles and remove members through backend member endpoints.
    - Test 3: invite/member actions respect owner-only semantics surfaced from backend errors instead of client-side fake gates.
  </behavior>
  <action>Implement production-ready basic mutation flows for create, edit, delete, and member management on the dashboard using the existing backend endpoints in `projects.py`. Add dedicated dialogs/components if needed, but keep all actions grounded in real API calls and backend ACL semantics. Surface owner/member management actions through typed mutations and refresh live data after success. If the current Phase 8 backend contract lacks a dedicated frontend-friendly detail shape, adapt the frontend client/parsing to the existing contract rather than reintroducing static placeholders or “for now” local state.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/frontend && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>Create/edit/delete/member operations are all initiated from the projects dashboard and each operation uses real backend endpoints, not local mock state.</acceptance_criteria>
  <done>User-facing dashboard CRUD and member-management truth from verification is fully reachable with live backend wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→projects API | User-entered search/form/member inputs cross into protected project endpoints |
| project owner UI→member mutations | Elevated owner actions can alter collaborator access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | T | `frontend/src/lib/api/projects.ts` | mitigate | Validate request/response shapes with zod before rendering or submitting mutations |
| T-10-18 | E | member-management UI | mitigate | Rely on backend ACL/owner checks and surface 403/404 responses without client-side bypass paths |
| T-10-19 | I | projects page empty/error states | mitigate | Do not leak unauthorized project details; reflect backend not-found/forbidden responses generically |
</threat_model>

<verification>
Run `cd frontend && pnpm lint && pnpm build`, then confirm the verification gap is closed by checking the projects page no longer uses static demo data and all CRUD/member actions route through backend APIs.
</verification>

<success_criteria>
- Verification gap #1 is closed in full.
- D-01, D-02, and D-03 remain fully implemented with live data.
- CRUD and member management are backend-wired, not local mock interactions.
</success_criteria>

<output>
After completion, create `/Users/mqcbook/Documents/github/my-source/PAPERY/.planning/phases/10-dashboard-admin-ui-quasarflow-stubs/10-05-SUMMARY.md`
</output>
