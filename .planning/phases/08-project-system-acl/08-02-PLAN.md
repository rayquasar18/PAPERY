---
phase: 08-project-system-acl
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - backend/app/repositories/project_member_repository.py
  - backend/app/api/dependencies.py
  - backend/app/services/project_service.py
  - backend/app/api/v1/projects.py
  - backend/tests/test_project_acl.py
autonomous: true
requirements:
  - PROJ-03
must_haves:
  truths:
    - "Owner has full project administration permissions"
    - "Editor can modify project metadata but cannot manage members"
    - "Viewer can read project data but cannot modify or administer it"
  artifacts:
    - path: "backend/app/repositories/project_member_repository.py"
      provides: "Membership role lookup and scoped access queries"
    - path: "backend/app/api/dependencies.py"
      provides: "Reusable project-role authorization dependencies"
    - path: "backend/tests/test_project_acl.py"
      provides: "Automated role-matrix verification"
  key_links:
    - from: "backend/app/api/dependencies.py"
      to: "backend/app/repositories/project_member_repository.py"
      via: "role resolution per project UUID and current user"
      pattern: "ProjectMemberRepository"
    - from: "backend/app/api/v1/projects.py"
      to: "backend/app/api/dependencies.py"
      via: "Depends for read/write/admin gates"
      pattern: "Depends\(require_project"
    - from: "backend/tests/test_project_acl.py"
      to: "backend/app/services/project_service.py"
      via: "matrix tests covering owner/editor/viewer capabilities"
      pattern: "editor|viewer|owner"
---

<objective>
Add reusable ACL enforcement for project-scoped authorization so every endpoint can rely on strict owner/editor/viewer semantics.

Purpose: Make project permissions explicit and reusable after the project CRUD foundation from Plan 01 exists, and before invites, membership mutation, and listing build on them.
Output: Membership repository helpers, project-role dependencies, ACL-aware service updates, and a dedicated ACL test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-project-system-acl/08-CONTEXT.md
@.planning/phases/08-project-system-acl/08-RESEARCH.md
@.planning/phases/08-project-system-acl/08-VALIDATION.md
@backend/app/api/dependencies.py
@backend/app/models/project.py
@backend/app/repositories/base.py
@backend/app/services/project_service.py
@backend/app/api/v1/projects.py

<interfaces>
From backend/app/models/project.py:
```python
class ProjectMemberRole(str, Enum):
    OWNER = "owner"
    EDITOR = "editor"
    VIEWER = "viewer"
```

From backend/app/api/dependencies.py:
```python
async def get_current_active_user(...) -> User
```

From backend/app/services/project_service.py:
```python
class ProjectService:
    async def get_project_for_user(...)
    async def update_project(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add membership lookup repository and ACL dependency contracts</name>
  <read_first>
    - backend/app/models/project.py
    - backend/app/repositories/base.py
    - backend/app/api/dependencies.py
    - backend/tests
    - .planning/phases/08-project-system-acl/08-VALIDATION.md
  </read_first>
  <files>backend/app/repositories/project_member_repository.py, backend/app/api/dependencies.py, backend/tests/test_project_acl.py</files>
  <behavior>
    - Test 1: owner passes read, write, and admin dependencies per D-01 and D-02.
    - Test 2: editor passes read/write but fails admin dependency per D-03.
    - Test 3: viewer passes read only and fails write/admin dependencies per D-04.
  </behavior>
  <action>Write `backend/tests/test_project_acl.py` first to establish the role matrix from D-01 through D-04, using the CRUD surface created in Plan 01 as the baseline. Create `ProjectMemberRepository` with helpers such as role lookup by `(project_uuid, user_id)`, read-scope existence checks, and owner-count helper shape needed by later member management. Extend `backend/app/api/dependencies.py` with reusable ACL factories or dependency callables for project read/write/admin gates; each dependency must load membership through the repository, allow owner/editor/viewer per the matrix, and raise `NotFoundError` or `ForbiddenError` consistently. Use explicit names such as `require_project_read_access`, `require_project_write_access`, and `require_project_admin_access`; do not inline role checks inside route bodies.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_project_acl.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_project_acl.py` covers owner/editor/viewer access matrix with automated assertions.
    - `backend/app/repositories/project_member_repository.py` exists with role lookup helpers for a project UUID and user.
    - `backend/app/api/dependencies.py` exports dedicated project read/write/admin dependencies instead of ad-hoc route logic.
    - ACL behavior matches D-02, D-03, and D-04 exactly with no simplified combined permission level.
  </acceptance_criteria>
  <done>Reusable ACL contracts exist and prove the strict permission matrix required by PROJ-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire project CRUD through ACL dependencies and scoped service access</name>
  <read_first>
    - backend/app/api/v1/projects.py
    - backend/app/services/project_service.py
    - backend/app/api/dependencies.py
    - backend/app/repositories/project_member_repository.py
    - backend/tests/test_project_acl.py
    - backend/tests/test_projects.py
  </read_first>
  <files>backend/app/services/project_service.py, backend/app/api/v1/projects.py, backend/tests/test_project_acl.py</files>
  <behavior>
    - Test 1: GET detail works for owner/editor/viewer but not outsiders.
    - Test 2: PATCH metadata works for owner/editor and rejects viewer/outsider.
    - Test 3: membership-management endpoints remain absent here, proving editor cannot manage membership by route surface or service method exposure.
  </behavior>
  <action>Update `ProjectService` and `backend/app/api/v1/projects.py` so the CRUD endpoints from Plan 01 consume the ACL dependencies rather than bespoke owner checks. Read operations must accept owner/editor/viewer; metadata writes must accept owner/editor only; project-level administrative delete should remain owner-only per D-02 and D-03. Keep service methods scoped and explicit: route handlers should receive the resolved project or membership context from dependencies and avoid duplicate repository exploration. Do not add invite/member routes yet; this plan is strictly about enforcing PROJ-03 on the existing CRUD surface.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_project_acl.py tests/test_projects.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Project detail endpoint is readable by owner/editor/viewer and not by outsiders.
    - Project update endpoint allows owner/editor and blocks viewer.
    - Project delete remains owner-only per D-02.
    - No route body contains duplicated manual role branching that bypasses the reusable dependency layer.
  </acceptance_criteria>
  <done>Project CRUD now enforces the full ACL matrix consistently, satisfying PROJ-03 with executable tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user→ACL dependency | Authenticated identity crosses into authorization decisions for project UUID access |
| ACL dependency→membership repository | Role resolution must not over-grant due to missing or ambiguous membership rows |
| routes→service methods | Endpoints must preserve dependency-derived permissions when calling project mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | E | `backend/app/api/dependencies.py` ACL factories | mitigate | Encode D-02/D-03/D-04 as explicit read/write/admin dependencies and test each role path in `tests/test_project_acl.py` |
| T-08-06 | I | `backend/app/repositories/project_member_repository.py` membership lookup | mitigate | Scope all lookups by project UUID and current user, returning no access on missing rows rather than permissive fallbacks |
| T-08-07 | T | `backend/app/api/v1/projects.py` update/delete handlers | mitigate | Replace ad-hoc owner checks with dependency-injected project access gates and keep delete owner-only |
| T-08-08 | R | ACL regression coverage | mitigate | Maintain a dedicated `tests/test_project_acl.py` suite so role regressions fail quickly during execution |
</threat_model>

<verification>
Run `cd backend && uv run pytest tests/test_project_acl.py tests/test_projects.py -q` and confirm role-specific access behavior remains stable.
</verification>

<success_criteria>
- Owner/editor/viewer permissions match the locked matrix exactly.
- Outsiders cannot read or mutate project resources by UUID.
- CRUD endpoints consume reusable ACL dependencies instead of repeating logic.
- ACL tests pass from `backend/tests/test_project_acl.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-system-acl/08-02-SUMMARY.md`
</output>
