---
phase: 08-project-system-acl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/project.py
  - backend/app/models/__init__.py
  - backend/app/repositories/project_repository.py
  - backend/app/schemas/project.py
  - backend/app/services/project_service.py
  - backend/app/api/v1/projects.py
  - backend/tests/test_projects.py
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
must_haves:
  truths:
    - "Authenticated user can create a project with name and description"
    - "Project owner can fetch and update their project metadata"
    - "Soft-deleted projects disappear from normal reads and lists immediately"
  artifacts:
    - path: "backend/app/models/project.py"
      provides: "Project ORM model with soft-delete support"
    - path: "backend/app/services/project_service.py"
      provides: "Project CRUD business logic with owner membership seeding"
    - path: "backend/app/api/v1/projects.py"
      provides: "Authenticated project create/detail/update/delete endpoints"
    - path: "backend/tests/test_projects.py"
      provides: "Integration coverage for create/update/soft-delete behaviors"
  key_links:
    - from: "backend/app/api/v1/projects.py"
      to: "backend/app/services/project_service.py"
      via: "route handlers instantiating ProjectService with AsyncSession"
      pattern: "ProjectService\(db\)"
    - from: "backend/app/services/project_service.py"
      to: "backend/app/repositories/project_repository.py"
      via: "repository-backed project persistence"
      pattern: "ProjectRepository"
    - from: "backend/app/services/project_service.py"
      to: "backend/app/models/project.py"
      via: "owner membership seeded on create"
      pattern: "owner"
---

<objective>
Deliver the project core slice: create, read, update, and soft-delete project records with immediate list exclusion and owner seeding.

Purpose: Establish the durable project container that all ACL and collaboration flows build on.
Output: Project model, repository, schemas, service methods, routes, and project CRUD integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-project-system-acl/08-CONTEXT.md
@.planning/phases/08-project-system-acl/08-RESEARCH.md
@.planning/phases/08-project-system-acl/08-VALIDATION.md
@backend/app/models/base.py
@backend/app/models/__init__.py
@backend/app/api/dependencies.py
@backend/app/services/tier_service.py
@backend/app/services/usage_service.py
@backend/app/core/exceptions/__init__.py

<interfaces>
From backend/app/api/dependencies.py:
```python
async def get_current_active_user(user: User = Depends(get_current_user)) -> User
class CheckUsageLimit:
    def __init__(self, metric: str) -> None: ...
```

From backend/app/models/base.py:
```python
class UUIDMixin:
    uuid: Mapped[uuid_pkg.UUID]
class TimestampMixin:
    created_at: Mapped[datetime]
    updated_at: Mapped[datetime | None]
class SoftDeleteMixin:
    deleted_at: Mapped[datetime | None]
```

From backend/app/services/usage_service.py:
```python
class UsageService:
    async def increment_usage(self, user_id: int, metric: str) -> int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project contracts, persistence, and CRUD tests</name>
  <read_first>
    - backend/app/models/base.py
    - backend/app/models/__init__.py
    - backend/app/models/user.py
    - backend/app/repositories/base.py
    - backend/tests
    - .planning/phases/08-project-system-acl/08-VALIDATION.md
  </read_first>
  <files>backend/app/models/project.py, backend/app/models/__init__.py, backend/app/repositories/project_repository.py, backend/app/schemas/project.py, backend/tests/test_projects.py</files>
  <behavior>
    - Test 1: authenticated user creating a project gets a persisted project plus seeded owner relationship per D-01, D-02, and PROJ-01.
    - Test 2: owner updating name/description succeeds, while a soft-deleted project no longer appears in repository-backed reads per D-05 and D-07.
    - Test 3: validation rejects blank project names and overly long names through schema constraints before service mutation.
  </behavior>
  <action>Create `Project`, `ProjectMemberRole`, and any minimal shared enums in `backend/app/models/project.py` so this plan defines the contracts downstream plans implement against. Model `Project` with `UUIDMixin`, `TimestampMixin`, and `SoftDeleteMixin`; include `owner_id` FK to `user.id`, `name` String(160), `description` Text nullable, and owner/member relationships. Register exports in `backend/app/models/__init__.py`. Create `ProjectRepository` with `get_by_uuid_for_owner_or_member` placeholder-safe read helpers and soft-delete-aware detail lookup using BaseRepository patterns. Create `backend/app/schemas/project.py` with `ProjectCreate`, `ProjectUpdate`, `ProjectRead`, and `ProjectListItemRead` contracts; enforce name length 1..160 and trim whitespace. Write `backend/tests/test_projects.py` first to cover project creation, owner seed behavior, update, and soft-delete exclusion; do not add invite/listing-specific cases yet. Use D-05/D-07 strictly: no hard delete path, no restore schema, no placeholder fields for future restore.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_projects.py::test_create_project_owner_seed tests/test_projects.py::test_owner_soft_delete_excluded_from_list -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/models/project.py` exports a role enum including `owner`, `editor`, and `viewer` per D-01.
    - `backend/app/models/project.py` defines a soft-deletable `Project` model with UUID public identifier and owner FK.
    - `backend/app/schemas/project.py` contains create/update/read contracts with concrete name validation bounds.
    - `backend/tests/test_projects.py` exists and includes automated coverage for owner seed and soft-delete exclusion.
    - No restore route, restore schema, or hard-delete repository helper is introduced for projects.
  </acceptance_criteria>
  <done>Project contracts and tests exist, fail/pass against real CRUD behavior targets, and fully cover PROJ-01 plus the soft-delete portion of PROJ-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement project CRUD service and authenticated routes</name>
  <read_first>
    - backend/app/api/dependencies.py
    - backend/app/services/admin_service.py
    - backend/app/services/tier_service.py
    - backend/app/services/usage_service.py
    - backend/app/schemas/auth.py
    - backend/app/api/v1/users.py
    - backend/app/core/exceptions/__init__.py
    - backend/app/repositories/project_repository.py
    - backend/tests/test_projects.py
  </read_first>
  <files>backend/app/services/project_service.py, backend/app/api/v1/projects.py, backend/tests/test_projects.py</files>
  <behavior>
    - Test 1: POST create enforces authenticated user plus `CheckUsageLimit("projects")`, persists the project, seeds owner membership, and increments usage after success.
    - Test 2: owner can GET/PATCH/DELETE their project; DELETE performs soft delete only and hidden projects return not found on subsequent normal reads.
    - Test 3: non-owner/non-member access never mutates or reveals the project through normal CRUD endpoints.
  </behavior>
  <action>Implement `ProjectService(db)` using repository-first data access only. Add methods for `create_project`, `get_project_for_user`, `update_project`, and `soft_delete_project`; raise `NotFoundError` for inaccessible resources to prevent IDOR disclosure, and call `UsageService.increment_usage(user.id, "projects")` only after a successful create. Seed owner membership during creation per D-02 so later ACL plans can rely on the membership table instead of ad-hoc owner checks. Create `backend/app/api/v1/projects.py` with POST `/projects`, GET `/projects/{uuid}`, PATCH `/projects/{uuid}`, and DELETE `/projects/{uuid}`; protect all with `get_current_active_user`, and put `CheckUsageLimit("projects")` on create. Return concrete response models from `backend/app/schemas/project.py`; do not add list, invite, or member-management routes in this plan.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_projects.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/project_service.py` is class-based DI and uses repositories instead of direct SQLAlchemy queries.
    - Create path seeds the owner role per D-02 and increments `projects` usage only after persistence.
    - Delete path calls soft-delete logic only per D-05 and normal reads exclude deleted records per D-07.
    - `backend/app/api/v1/projects.py` exposes POST/GET/PATCH/DELETE endpoints with authenticated user dependency.
    - Inaccessible project UUIDs resolve consistently as `NotFoundError` rather than leaking resource existence.
  </acceptance_criteria>
  <done>Authenticated users can create and manage their own projects end-to-end, with soft delete semantics and test proof for PROJ-01 and PROJ-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→projects API | Untrusted project create/update payloads and cookies cross into authenticated backend handlers |
| projects API→service layer | Route-level identity and quota checks must be preserved before business mutations |
| service→database | Project UUID lookups and soft-delete writes must not bypass repository filtering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | E | `backend/app/api/v1/projects.py` create/update/delete endpoints | mitigate | Require `get_current_active_user`, apply `CheckUsageLimit("projects")` on create, and resolve user-scoped detail through service methods only |
| T-08-02 | I | `backend/app/services/project_service.py` project detail access | mitigate | Return `NotFoundError` for unauthorized UUID access and use owner/member-scoped repository lookups rather than raw `get(uuid=...)` |
| T-08-03 | T | `backend/app/repositories/project_repository.py` delete path | mitigate | Route deletion through `soft_delete()` only; add tests proving deleted rows disappear from normal reads immediately |
| T-08-04 | R | `backend/tests/test_projects.py` CRUD regression coverage | mitigate | Add automated integration tests for owner seed, update, and soft-delete exclusion before considering plan complete |
</threat_model>

<verification>
Run `cd backend && uv run pytest tests/test_projects.py -q` and confirm project create/update/delete flows stay green without introducing restore behavior.
</verification>

<success_criteria>
- Authenticated create returns persisted project data with owner relationship seeded.
- Owner can fetch, edit, and soft-delete project metadata.
- Soft-deleted projects disappear from normal CRUD reads immediately.
- Automated project CRUD tests pass from `backend/tests/test_projects.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-system-acl/08-01-SUMMARY.md`
</output>
