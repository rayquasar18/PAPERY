---
phase: 08-project-system-acl
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - backend/app/repositories/project_repository.py
  - backend/app/schemas/project.py
  - backend/app/services/project_service.py
  - backend/app/api/v1/projects.py
  - backend/tests/test_project_listing.py
autonomous: true
requirements:
  - PROJ-06
must_haves:
  truths:
    - "User can retrieve one project list containing both owned and shared projects"
    - "Each list item indicates whether the relationship is owned or shared"
    - "Search filters by project name and default sort is updated_at descending"
  artifacts:
    - path: "backend/app/repositories/project_repository.py"
      provides: "Owned+shared listing query with relationship projection"
    - path: "backend/app/schemas/project.py"
      provides: "List response contracts including relationship_type"
    - path: "backend/app/api/v1/projects.py"
      provides: "Single list endpoint with search and pagination"
    - path: "backend/tests/test_project_listing.py"
      provides: "Listing/search/sort integration coverage"
  key_links:
    - from: "backend/app/api/v1/projects.py"
      to: "backend/app/services/project_service.py"
      via: "GET /projects list handler"
      pattern: "list_projects"
    - from: "backend/app/services/project_service.py"
      to: "backend/app/repositories/project_repository.py"
      via: "single merged owned/shared query"
      pattern: "relationship_type"
    - from: "backend/tests/test_project_listing.py"
      to: "backend/app/schemas/project.py"
      via: "assert list items expose owned/shared plus updated_at ordering"
      pattern: "relationship_type|updated_at"
---

<objective>
Ship the frontend-friendly project list API with owned/shared merging, relationship labeling, name search, and deterministic default sorting.

Purpose: Complete the main project discovery surface that the dashboard will consume without requiring multiple endpoints or client-side merging, after Plans 01 and 02 establish the CRUD and ACL baseline.
Output: Listing query, list schemas, route parameters, and integration tests for search/sort/pagination.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-project-system-acl/08-CONTEXT.md
@.planning/phases/08-project-system-acl/08-RESEARCH.md
@.planning/phases/08-project-system-acl/08-VALIDATION.md
@backend/app/models/project.py
@backend/app/repositories/project_repository.py
@backend/app/services/project_service.py
@backend/app/api/v1/projects.py

<interfaces>
From backend/app/schemas/project.py:
```python
class ProjectListItemRead(BaseModel):
    uuid: uuid_pkg.UUID
    name: str
    description: str | None
    relationship_type: Literal["owned", "shared"]
    updated_at: datetime | None
```

From backend/app/models/project.py:
```python
class Project(Base, UUIDMixin, TimestampMixin, SoftDeleteMixin): ...
class ProjectMemberRole(str, Enum): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add listing query and response contracts for owned plus shared projects</name>
  <read_first>
    - backend/app/repositories/project_repository.py
    - backend/app/schemas/project.py
    - backend/app/models/project.py
    - backend/tests
    - .planning/phases/08-project-system-acl/08-VALIDATION.md
  </read_first>
  <files>backend/app/repositories/project_repository.py, backend/app/schemas/project.py, backend/tests/test_project_listing.py</files>
  <behavior>
    - Test 1: one repository-backed list includes both owned and shared projects per D-11.
    - Test 2: each returned item includes `relationship_type` equal to `owned` or `shared` per D-12.
    - Test 3: search matches project name only and default order is `updated_at DESC` per D-13 and D-14.
  </behavior>
  <action>Write `backend/tests/test_project_listing.py` first. Extend `ProjectRepository` with a single listing method that merges owned projects and accepted shared memberships into one result set without duplicate rows, excludes soft-deleted projects immediately per D-07, filters optional `search` against project name, and orders by `updated_at DESC` by default. Add list schemas in `backend/app/schemas/project.py` for item and paginated response shapes; each item must expose `relationship_type` exactly as `owned` or `shared` per D-12. Choose reasonable pagination defaults under Claude discretion, but make them explicit in the schema/route contract and cap page size to prevent unbounded responses.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_project_listing.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one listing query path exists for owned+shared results per D-11.
    - Each list item exposes `relationship_type` with only `owned` or `shared` values per D-12.
    - Search targets project name and not unrelated fields per D-13.
    - Default ordering is `updated_at DESC` per D-14.
    - Soft-deleted projects remain excluded from list results.
  </acceptance_criteria>
  <done>Repository/query contracts fully capture PROJ-06 and prove behavior with automated listing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the single list endpoint and wire service pagination</name>
  <read_first>
    - backend/app/services/project_service.py
    - backend/app/api/v1/projects.py
    - backend/app/repositories/project_repository.py
    - backend/tests/test_project_listing.py
  </read_first>
  <files>backend/app/services/project_service.py, backend/app/api/v1/projects.py, backend/tests/test_project_listing.py</files>
  <behavior>
    - Test 1: GET `/projects` returns owned and shared projects in one payload with pagination metadata.
    - Test 2: search query narrows by project name while preserving access scoping.
    - Test 3: results are sorted newest-updated first when no explicit sort is passed.
  </behavior>
  <action>Implement a service method such as `list_projects_for_user()` that delegates to the repository query and returns a stable paginated response. Add GET `/projects` to `backend/app/api/v1/projects.py` if Plan 01 only created detail routes, or extend the existing route file if the endpoint already exists. The endpoint must use one response contract for owned and shared projects, expose search/page/per_page query params, and never split into separate `/owned` and `/shared` endpoints per D-11. Keep sorting server-side with `updated_at DESC` default; do not offload relationship tagging or merge logic to the frontend.</action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/PAPERY/backend && uv run pytest tests/test_project_listing.py tests/test_projects.py -q</automated>
  </verify>
  <acceptance_criteria>
    - GET `/projects` is the single list endpoint for both owned and shared projects.
    - Response contains pagination metadata plus list items with `relationship_type`.
    - Search and sorting behavior match D-13 and D-14.
    - Service and route code do not create separate owned/shared API surfaces.
  </acceptance_criteria>
  <done>User-facing project discovery is complete for PROJ-06 and ready for frontend consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user→list endpoint | Query params and identity cross into a scoped project discovery query |
| service→repository listing query | Merge logic must not leak unrelated shared projects or duplicate rows |
| API response→frontend consumer | Relationship labeling becomes an authorization-adjacent contract that the UI will trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | I | `backend/app/repositories/project_repository.py` merged listing query | mitigate | Scope owned/shared predicates explicitly, exclude soft-deleted records, and test that unrelated projects never appear |
| T-08-14 | T | `backend/app/schemas/project.py` list contracts | mitigate | Constrain `relationship_type` to `owned|shared` and keep server-side assignment authoritative |
| T-08-15 | D | `backend/app/api/v1/projects.py` pagination params | mitigate | Cap `per_page` and use explicit defaults to avoid unbounded list responses |
| T-08-16 | R | Listing/search regressions | mitigate | Add `tests/test_project_listing.py` with owned/shared/search/sort assertions covering the exact locked decisions |
</threat_model>

<verification>
Run `cd backend && uv run pytest tests/test_project_listing.py tests/test_projects.py -q` and confirm merged listing behavior remains correct.
</verification>

<success_criteria>
- One list endpoint returns both owned and shared projects.
- Every item includes `relationship_type` for frontend rendering.
- Search filters by project name and default ordering is `updated_at DESC`.
- Listing tests pass from `backend/tests/test_project_listing.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-system-acl/08-04-SUMMARY.md`
</output>
