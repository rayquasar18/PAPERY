# Quick Task 260411-q6r: Post-Phase-6 Comprehensive System Audit

**Created:** 2026-04-11
**Status:** Ready for execution
**Mode:** quick (parallel audit agents)

---

## Task 1: Move billing schemas from API to schemas module

**Files:** `backend/app/api/v1/billing.py`, `backend/app/schemas/billing.py` (new)
**Action:** Extract `CheckoutRequest`, `CheckoutResponse`, `PortalResponse` from `billing.py` API file into a proper `schemas/billing.py` module. Add `SubscriptionStatusResponse` schema for the `/subscription` endpoint (currently returns untyped dict). Update imports in billing router.
**Verify:** `billing.py` imports from `app.schemas.billing`, no inline Pydantic models remain in API files.
**Done:** All billing schemas live in `schemas/billing.py`.

## Task 2: Replace custom rate limiting with slowapi

**Files:** `backend/app/utils/rate_limit.py`, `backend/app/main.py`, `backend/app/api/v1/auth.py`, `backend/app/api/v1/users.py`, `backend/pyproject.toml`
**Action:** Install `slowapi` as dependency. Create `backend/app/middleware/rate_limit.py` with SlowAPI Limiter configured to use Redis. Replace all manual `check_rate_limit()` calls in auth.py and users.py with `@limiter.limit()` decorators. Remove or refactor `utils/rate_limit.py`. Register SlowAPI exception handler and middleware in `main.py`.
**Verify:** All rate limits use slowapi decorators, `utils/rate_limit.py` is either removed or deprecated, tests pass.
**Done:** Rate limiting is middleware-driven via slowapi.

## Task 3: Fix inconsistent service patterns in auth router

**Files:** `backend/app/api/v1/auth.py`, `backend/app/services/auth_service.py`
**Action:** Auth router mixes class-based `AuthService(db)` with module-level `auth_service.change_password()` and `auth_service.oauth_login_or_register()`. Refactor all module-level function calls to use the `AuthService` class instance consistently. Move `change_password`, `set_password`, `oauth_login_or_register` to class methods on `AuthService` if not already.
**Verify:** All auth router handlers use `service = AuthService(db)` followed by `service.method()`. No module-level function calls remain.
**Done:** Auth router uses consistent class-based service pattern.

## Task 4: Add proper response schema for subscription endpoint + fix Stripe API deprecations

**Files:** `backend/app/api/v1/billing.py`, `backend/app/schemas/billing.py`, `backend/app/services/stripe_service.py`
**Action:** (a) Add `SubscriptionStatusResponse` Pydantic schema with typed fields. Update `/billing/subscription` to use `response_model=SubscriptionStatusResponse`. (b) Fix deprecated `stripe.error.SignatureVerificationError` → `stripe.SignatureVerificationError` and `stripe.error.StripeError` → `stripe.StripeError` (Stripe SDK v6+).
**Verify:** `/subscription` endpoint has typed response_model. Stripe imports use non-deprecated paths.
**Done:** All endpoints have proper response schemas, Stripe API calls use current SDK patterns.
